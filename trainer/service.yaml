apiVersion: v1
kind: Service
metadata:
  name: logistic-trainer
  namespace: logistic
spec:
  selector:
    app: logistic-trainer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000