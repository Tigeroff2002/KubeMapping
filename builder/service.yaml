apiVersion: v1
kind: Service
metadata:
  name: logistic-builder
  namespace: logistic
spec:
  selector:
    app: logistic-builder
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8090 