apiVersion: v1
kind: Service
metadata:
  name: logistic-payment
  namespace: logistic
spec:
  selector:
    app: logistic-payment
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8070 