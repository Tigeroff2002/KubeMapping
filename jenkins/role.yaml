apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: logistic
  name: jenkins
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["create", "get", "list", "watch", "delete"]
- apiGroups: [""]
  resources: ["pods/exec"]
  verbs: ["create", "get"]