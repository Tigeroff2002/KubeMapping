apiVersion: v1
kind: Service
metadata:
  name: camunda-bpmn-orchestrator
  namespace: logistic
spec:
  selector:
    app: camunda-bpmn-orchestrator
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP